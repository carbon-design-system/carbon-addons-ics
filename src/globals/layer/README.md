## ICS shadows & elevation

carbon-addons-ics includes its own values for box-shadow styles.

### Usage

To use ICS box shadow styles in your project, include the layer mixin.

```scss
@include layer('raised');
```